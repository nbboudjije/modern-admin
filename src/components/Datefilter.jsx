import React from "react";
import { DateRangePicker } from "@tremor/react";

const Datefilter = () => {
  return <DateRangePicker className="max-w-sm mx-auto" enableSelect={false} />;
};

export default Datefilter;
